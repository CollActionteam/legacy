import React from "react";
import Layout from "../../components/Layout";

export default () => (
    <Layout>
        <h1>Start Project</h1>
        <p>This will be the start project page.</p>
    </Layout>
);