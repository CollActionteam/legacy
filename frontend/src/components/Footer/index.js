import React from "react";

export default () => (
  <div style={{ padding: "1rem" }}>
    This will be the footer.
    <p style={{ color: "var(--c-green)" }}>
      We can use global CSS properties for global styling such as green text.
    </p>
  </div>
);
